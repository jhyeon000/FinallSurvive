using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class youarestupid : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //e bao mungchung a !
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
